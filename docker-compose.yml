
# ----- Use Only for working with Local Mongo Compass -----
services:
  mongodb:
    image: mongo
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo:/data/db

volumes:
  mongo:
